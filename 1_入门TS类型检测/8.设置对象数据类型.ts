// parameter:object 设置对象类型，可以是function函数、array数组、object对象

// 对象的键只会是字符串类型，所以无需设置，但可以设置对象中值的类型
// parameter:{id:number, name:string, flag:boolean} 设置对象中值为基本数据类型

// parameter:{key?:string} 设置对象中非必需属性，可不传，若传入则限制属性值为字符串类型